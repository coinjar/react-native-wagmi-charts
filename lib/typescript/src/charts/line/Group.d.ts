import { ReactNode } from 'react';
import { ViewProps } from 'react-native';
declare type Props = {
    children: ReactNode;
} & ViewProps;
export declare function LineChartGroup({ children, ...props }: Props): JSX.Element;
export {};
